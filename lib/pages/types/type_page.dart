import 'package:flutter/material.dart';

class TypePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Typ')),
        body: Center(
          child: Text('Type'),
        ));
  }
}
